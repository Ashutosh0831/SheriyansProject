import FaceExpression from "../components/FaceExpression";

const FaceExpress = ({ onClick }) => {
  return (
    <div>
      <FaceExpression onClick={onClick} />
    </div>
  );
};

export default FaceExpress;
